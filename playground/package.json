{
  "name": "compose-svelted-playground",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "npm exec -- svelte-check --tsconfig ./tsconfig.app.json && npm exec -- tsc -p ./tsconfig.node.json"
  },
  "dependencies": {
    "@danielito1996/compose-svelted": "file:.."
  }
}
